using UnityEngine;

public class PlayCard : MonoBehaviour
{
	//[SerializeField] private SO_Card _card;

	protected void Update()
	{
		if (Input.GetKeyDown(KeyCode.Space))
		{
			//_card._cardAction.DoAction();
		}
	}
}
